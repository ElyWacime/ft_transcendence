{
  "name": "fastify-pong",
  "version": "1.0.0",
  "description": "Server-driven Pong with Fastify + WebSocket (TypeScript)",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "lint": "echo \"no lint configured\""
  },
  "dependencies": {
    "fastify": "^4.23.0",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@fastify/websocket": "^11.2.0",
    "@types/node": "^20.4.2",
    "@types/ws": "^8.18.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.1.6"
  }
}
